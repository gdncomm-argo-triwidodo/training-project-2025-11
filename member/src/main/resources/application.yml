server:
  port: 8081
  servlet:
    context-path: /member

spring:
  application:
    name: member-service
  datasource:
    url: jdbc:postgresql://homelab.gosoft.web.id:5432/member
    driverClassName: org.postgresql.Driver
    username: admin_db
    password: Password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

jwt:
  secret: ${JWT_SECRET:MyVeryLongDefaultSecretKeyMustBeLongEnoughToSatisfyHS256Requirements1234567890}

# JavaMelody Monitoring Configuration
javamelody:
  enabled: true
  excluded-datasources: none
  spring-monitoring-enabled: true
  init-parameters:
    monitoring-path: /monitoring
    storage-directory: /tmp/javamelody/member-service
    resolution-seconds: 60
    log: true
    url-exclude-pattern: (/webjars/.*|/css/.*|/js/.*|/images/.*|/favicon\.ico|/actuator/.*)
